streamlit>=1.61
plotly>=6.0
pandas>=2.2

# Added as the real backend lands (see plan):
# duckdb          — in-process warehouse
# sqlglot         — SQL parsing for the governance allow-list
# openai          — DeepSeek client (OpenAI-compatible)
# python-dotenv   — loads DEEPSEEK_API_KEY from .env
# Faker           — synthetic data generation
